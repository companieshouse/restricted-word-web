interface PaginationOptions {
    previousPage: number;
    nextPage: number;
    currentPage: number;
    totalPages: number;
    numResults: number;
    startOfPage: number;
    endOfPage: number;
}

// eslint-disable-next-line no-undef
export default PaginationOptions;
